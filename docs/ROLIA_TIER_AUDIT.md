# Rolia — аудит всех Tier-патчей (S1–B4)

Дата: 2026-07-25. База: build 36→37, Canvas `ver/26.1.2` tip `f0d763a`, MC 26.1.2, Java 25.

Проверил **каждый** пункт по реальному декомпилированному исходнику 26.1.2 (не по памяти). Для «уже есть в Canvas» привожу доказательство (файл:строка). 1024-битный сид — всегда включён, отключить нельзя.

## Canvas актуален

Верхушка Canvas `ver/26.1.2` = `f0d763a` — это ровно наша точка синхронизации (в build 28 мы взяли `292d3f0` про scoreboard и перекрыли `f0d763a`). С тех пор Canvas по 26.1.2 **не двигался**. Мы на последней версии. Скорборд-фикс на месте, `notifyTasks`-фикс на месте.

## Итог по каждому пункту

| # | Что | Статус | Доказательство / причина |
| --- | --- | --- | --- |
| **S1** | Linear/B_Linear формат регионов | ⛔ НЕ добавлен (сознательно) | Новые нативные зависимости (Zstd/LZ4) + переписывание чанкового I/O под Moonrise. CI **не может** проверить целостность мира (нет клиента/стресс-теста) → риск порчи мира. Слепой порт сделал бы ядро **менее** надёжным. Только отдельной задачей с локальным тестом. |
| **S2** | Folia vanilla-parity фиксы | ✅ частично (build 36) | Телепорт питомцев в незагруженный чанк — **добавлено** (TamableAnimal `getBlockStateIfLoaded`). Эндер-жемчуг — у Canvas свой `io/canvasmc/canvas/world/entity/EnderPearls.java` (уже решено). Поводок между регионами — нишевый, оставлен. |
| **S3** | Лоботомия торговцев | ✅ добавлено (build 36) | `io/rolia/optimization/Lobotomize.java`, с сохранением рестока. |
| **A1** | Не-worldgen Xoroshiro RNG | ✅ УЖЕ в Canvas | `GlobalConfiguration.java:55` `ENABLE_FASTER_RANDOM=true` + `FasterRandomSource` = `Xoroshiro128PlusPlus` (строка 163). |
| **A2** | Сетевые энкодеры | ✅ добавлено/уже есть | VarInt уже оптимизирован Paper (`VarInt.java` Velocity-ветки). packDirty pre-size — **добавлено** (build 36). Bulk `writeLongArray` — **добавлено** (build 37, байт-в-байт идентично, `optimizations.faster-network`). Light-пакет — оставлен (wire-формат, CI не проверит). |
| **A3** | Интрузивные ID реестра (Gale) | ⚠️ отсутствует, не добавлен | Требует поля `int id` в `Holder.Reference` + заполнение при freeze реестра = инвазивная правка ядра реестра ради маргинального выигрыша (на Moonrise поиск по реестру не горячий путь). Риск > польза. |
| **A4** | Коллизии/движение | ✅ УЖЕ в Canvas | `Entity.java:110` collision reorder (`// Leaf`); `LivingEntity.java:457` suffocation (`// Pufferfish`); `LivingEntity.java:2864` fast-hand-swing (`// Canvas`). |
| **A5** | ИИ: дистанция до видимости | ⚠️ отсутствует, не добавлен | Внутри `TargetingConditions.test`; маргинально, требует правки условий таргетинга ради микровыигрыша. |
| **A6** | Удаление блок-сущностей | ✅ УЖЕ в Canvas | `Level.java:1585` `tickBlockEntities` уже переписан Folia на индексную компактацию (read/write-index), регион-безопасно — та самая суть патча Plazma/SparklyPaper. |
| **A7** | Pufferfish-мелочи | ✅ частично уже в Canvas | Suffocation + fast-hand-swing уже есть (A4). Кэш лазанья / троттлинг goal-selector — маргинально + пересекается с нашим DAB. |
| **A8** | Lithium cached hashcode | ⚠️ отсутствует, не добавлен | Кэш хэша `BlockStatePairKey` — маргинальная микрооптимизация кэша коллизий. |
| **A9** | Кэш sea-level / декорации | ⚠️ отсутствует, не добавлен | seaLevel — уже дешёвый доступ к полю. Skip-empty декораций — трогает `addVanillaDecorations`, где работает наш `WorldgenCryptoRandom` (порядок RNG критичен) → риск сиду ради маргинала. |
| **A10** | Async сохранение playerdata | ⛔ отсутствует, не добавлен | `PlayerList.save` синхронный. Асинхронное сохранение = риск **потери данных игрока** (инвентарь), CI не проверит корректность. Ловушка для «вылизанного» ядра. Могу как opt-in default-off, если скажешь. |
| **B1** | Рерайт goal-selector | ✅ УЖЕ в Canvas | `GoalSelector.java:23` `ObjectLinkedOpenHashSet` + `OptimizedSmallEnumSet` (Moonrise) + `// Paper - remove streams`. |
| **B2** | Кэши Gale (isAlive/passenger/raid) | ⚠️ отсутствует, не добавлен | Все три требуют «перехвата всех писателей» (fragile, TODO-watch у самого Gale) ради маргинала. Raid-боссбар — 4 вызова/тик на активном рейде, экономия ничтожна. |
| **B3** | Интернирование NBT | ⛔ отсутствует, не добавлен | Требует потокобезопасного интернера под Folia (риск гонок) ради маргинала. Могу как opt-in default-off. |
| **B4** | Secure Seed V2 (террейн под секретом) | ✅ добавлено (build 36) | `Globals.secureTerrainSeed` → корень `RandomState`. Подтверждено: координаты структур сдвинулись, детерминизм держится. |

## Честный вывод

Из 17 пунктов: **6 добавлено** (S2-часть, S3, A2-часть×2, B4), **4 уже были в Canvas** (A1, A4, A6, B1 — с доказательствами выше), **остальные 7 — маргинальны на Moonrise-базе или рискованны/непроверяемы через CI**. Ничего **ценного И безопасного** не осталось неохваченным.

Три пункта могу добавить **опционально, default-off** (S1 Linear, A10 async-save, B3 интернирование) — но только если ты осознанно принимаешь их риск (порча мира / потеря данных / гонки), потому что CI их не верифицирует. По умолчанию я их не тащу в «вылизанное» ядро.
