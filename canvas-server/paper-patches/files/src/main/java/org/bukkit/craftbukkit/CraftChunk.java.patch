--- a/src/main/java/org/bukkit/craftbukkit/CraftChunk.java
+++ b/src/main/java/org/bukkit/craftbukkit/CraftChunk.java
@@ -136,10 +_,13 @@
         int index = 0;
         ChunkAccess chunk = this.getHandle(ChunkStatus.FULL);
 
-        BlockState[] states = new BlockState[chunk.blockEntities.size()];
-        for (BlockPos pos : chunk.blockEntities.keySet()) {
-            states[index++] = CraftBlock.at(this.level, pos).getState(useSnapshot);
+        // Canvas start - optimize block entity fetching
+        net.minecraft.world.level.block.entity.BlockEntity[] blockEntities = chunk.canvas$getAllBlockEntities();
+        BlockState[] states = new BlockState[blockEntities.length];
+        for (net.minecraft.world.level.block.entity.BlockEntity blockEntity : blockEntities) {
+            states[index++] = CraftBlock.at(this.level, blockEntity.getBlockPos()).getState(useSnapshot);
         }
+        // Canvas end - optimize block entity fetching
 
         return states;
     }
@@ -154,8 +_,11 @@
         ChunkAccess chunk = this.getHandle(ChunkStatus.FULL);
 
         List<BlockState> states = new ArrayList<>();
-        for (BlockPos pos : chunk.blockEntities.keySet()) {
-            Block block = CraftBlock.at(this.level, pos);
+        // Canvas start - optimize block entity fetching
+        net.minecraft.world.level.block.entity.BlockEntity[] blockEntities = chunk.canvas$getAllBlockEntities();
+        for (net.minecraft.world.level.block.entity.BlockEntity blockEntity : blockEntities) {
+            Block block = CraftBlock.at(this.level, blockEntity.getBlockPos());
+        // Canvas end - optimize block entity fetching
             if (blockPredicate.test(block)) {
                 states.add(block.getState(useSnapshot));
             }
@@ -193,7 +_,7 @@
     @Override
     public boolean isSlimeChunk() {
         // 987234911L is taken from Slime when seeing if a slime can spawn in a chunk
-        return this.level.paperConfig().entities.spawning.allChunksAreSlimeChunks || WorldgenRandom.seedSlimeChunk(this.getX(), this.getZ(), this.getWorld().getSeed(), level.spigotConfig.slimeSeed).nextInt(10) == 0; // Paper
+        return this.level.paperConfig().entities.spawning.allChunksAreSlimeChunks || io.rolia.secureseed.WorldgenCryptoRandom.seedSlimeChunk(this.getX(), this.getZ()).nextInt(10) == 0; // Paper // Rolia - the Bukkit API must agree with where slimes actually spawn (computed directly, never via getHandle(), which would force-load the chunk)
     }
 
     @Override
