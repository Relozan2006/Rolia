--- a/net/minecraft/world/level/block/entity/HopperBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/HopperBlockEntity.java
@@ -95,6 +_,7 @@
         if (!this.tryLoadLootTable(input)) {
             ContainerHelper.loadAllItems(input, this.items);
         }
+        rolia$invalidateFullState(this); // Canvas - optimize hoppers // Rolia - invalidate, don't recompute
 
         this.cooldownTime = input.getIntOr("TransferCooldown", -1);
     }
@@ -117,7 +_,25 @@
     @Override
     public ItemStack removeItem(final int slot, final int count) {
         this.unpackLootTable(null);
-        return ContainerHelper.removeItem(this.getItems(), slot, count);
+        // Canvas start - optimize hoppers
+        ItemStack retVal = ContainerHelper.removeItem(this.getItems(), slot, count);
+        rolia$invalidateFullState(this); // Rolia - invalidate, don't recompute
+        return retVal;
+        // Canvas end - optimize hoppers
     }
+    // Rolia start - keep the cached fullState correct on removeItemNoUpdate
+    // HopperBlockEntity does not override removeItemNoUpdate, so Canvas's cache sweep
+    // (loadAdditional / removeItem / setItem / setBlockState / clearContent / setChanged /
+    // setItems) missed it. Anything draining a hopper through this Container method left a
+    // stale HOPPER_IS_FULL or HOPPER_EMPTY value behind, and the hopper silently stopped
+    // pushing or pulling until the chunk was reloaded.
+
+    @Override
+    public ItemStack removeItemNoUpdate(final int slot) {
+        final ItemStack removed = super.removeItemNoUpdate(slot);
+        rolia$invalidateFullState(this); // Rolia - invalidate, don't recompute
+        return removed;
+    }
+    // Rolia end - keep the cached fullState correct on removeItemNoUpdate
 
     @Override
@@ -125,13 +_,24 @@
         this.unpackLootTable(null);
         this.getItems().set(slot, itemStack);
         itemStack.limitSize(this.getMaxStackSize(itemStack));
+        rolia$invalidateFullState(this); // Canvas - optimize hoppers // Rolia - invalidate, don't recompute
     }
 
     @Override
     public void setBlockState(final BlockState blockState) {
         super.setBlockState(blockState);
         this.facing = blockState.getValue(HopperBlock.FACING);
-    }
+        rolia$invalidateFullState(this); // Canvas - optimize hoppers // Rolia - invalidate, don't recompute
+        // Canvas - TODO - optimize hoppers - cache is locked and run tick skip system through here?
+    }
+    // Canvas start - optimize hoppers
+
+    @Override
+    public void clearContent() {
+        super.clearContent();
+        rolia$invalidateFullState(this); // Rolia - invalidate, don't recompute
+    }
+    // Canvas end - optimize hoppers
 
     @Override
     protected Component getDefaultName() {
@@ -156,8 +_,39 @@
     private static final int HOPPER_EMPTY = 0;
     private static final int HOPPER_HAS_ITEMS = 1;
     private static final int HOPPER_IS_FULL = 2;
-
-    private static int getFullState(final HopperBlockEntity hopper) {
+    // Canvas start - optimize hoppers
+    private int fullState = -1;
+    // Rolia start - invalidate the cache on mutation instead of eagerly recomputing it
+    // canvas$recomputeFullState() was called from loadAdditional, removeItem, removeItemNoUpdate,
+    // setItem, setBlockState, clearContent, setItems AND setChanged(). A single hopper transfer
+    // trips several of those in one tick, so the item scan ran N times per tick to produce a value
+    // that is read at most once, by pushItemsTick. That read already has a lazy
+    // `if (entity.fullState == -1) canvas$recomputeFullState(entity);` guard, and fullState is
+    // private with no other reader anywhere in the tree, so marking the cache dirty is enough:
+    // N mutations now cost one recompute instead of N.
+    //
+    // Behaviour is unchanged. Eager caching pinned the value to the instant of the last tracked
+    // mutation; lazy recompute pins it to the instant of the read, which is what vanilla's
+    // getFullState() did. Between those two instants only untracked mutations can occur, so lazy
+    // is never staler than eager, and is exactly vanilla whenever a tracked mutation happened.
+    //
+    // unpackLootTable(null) is kept: canvas$recomputeFullState() ran it at each of these sites, and
+    // dropping it would move when a loot-table hopper rolls its contents. It is a couple of field
+    // reads and a branch in the common case, next to nothing beside the scan we are deferring.
+    private static void rolia$invalidateFullState(final HopperBlockEntity hopper) {
+        hopper.unpackLootTable(null);
+        hopper.fullState = -1;
+    }
+    // Rolia end - invalidate the cache on mutation instead of eagerly recomputing it
+
+    @Override
+    public void setChanged() {
+        super.setChanged();
+        rolia$invalidateFullState(this); // Rolia - was: canvas$recomputeFullState(this)
+    }
+
+    private static void canvas$recomputeFullState(final HopperBlockEntity hopper) {
+    // Canvas end - optimize hoppers
         hopper.unpackLootTable(null);
 
         final List<ItemStack> hopperItems = hopper.items;
@@ -174,18 +_,20 @@
 
             if (!full) {
                 // can't be full
-                return HOPPER_HAS_ITEMS;
+                hopper.fullState = HOPPER_HAS_ITEMS; // Canvas - optimize hoppers
+                return; // Canvas - optimize hoppers
             }
 
             empty = false;
 
             if (stack.getCount() != stack.getMaxStackSize()) {
                 // can't be full or empty
-                return HOPPER_HAS_ITEMS;
+                hopper.fullState = HOPPER_HAS_ITEMS; // Canvas - optimize hoppers
+                return; // Canvas - optimize hoppers
             }
         }
 
-        return empty ? HOPPER_EMPTY : (full ? HOPPER_IS_FULL : HOPPER_HAS_ITEMS);
+        hopper.fullState = empty ? HOPPER_EMPTY : (full ? HOPPER_IS_FULL : HOPPER_HAS_ITEMS); // Canvas - optimize hoppers
     }
     // Paper end - Perf: Optimize Hoppers
 
@@ -198,7 +_,8 @@
 
         if (!entity.isOnCooldown() && state.getValue(HopperBlock.ENABLED)) {
             boolean changed = false;
-            final int fullState = getFullState(entity); // Paper - Perf: Optimize Hoppers
+            if (entity.fullState == -1) canvas$recomputeFullState(entity); // Canvas - optimize hoppers
+            final int fullState = entity.fullState; // Paper - Perf: Optimize Hoppers // Canvas - optimize hoppers
             if (fullState != HOPPER_EMPTY) { // Paper - Perf: Optimize Hoppers
                 changed = ejectItems(level, pos, entity);
             }
@@ -700,7 +_,7 @@
         final boolean optimizeEntities
     ) {
         // Paper end - Perf: Optimize Hoppers
-        Container result = getBlockContainer(level, pos, state);
+        Container result = state.hasBlockEntity() || state.getBlock() instanceof WorldlyContainerHolder ? getBlockContainer(level, pos, state) : null; // Canvas - optimize hoppers - if the block has no block entity, why check it
         if (result == null && (!optimizeEntities || !level.paperConfig().hopper.ignoreOccludingBlocks || !state.getBukkitMaterial().isOccluding())) { // Paper - Perf: Optimize Hoppers
             result = getEntityContainer(level, x, y, z);
         }
@@ -717,7 +_,7 @@
 
         if (state.hasBlockEntity() && level.getBlockEntity(pos) instanceof Container container) {
             if (container instanceof ChestBlockEntity && block instanceof ChestBlock) {
-                container = ChestBlock.getContainer((ChestBlock)block, state, level, pos, true);
+                container = ChestBlock.canvas$getContainerForHoppers((ChestBlock)block, state, level, pos, true, container); // Canvas - optimize hoppers
             }
 
             return container;
@@ -777,6 +_,7 @@
     @Override
     protected void setItems(final NonNullList<ItemStack> items) {
         this.items = items;
+        rolia$invalidateFullState(this); // Canvas - optimize hoppers // Rolia - invalidate, don't recompute
     }
 
     public static void entityInside(final Level level, final BlockPos pos, final BlockState blockState, final Entity entity, final HopperBlockEntity hopper) {
