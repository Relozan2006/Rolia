--- a/net/minecraft/world/level/chunk/ChunkAccess.java
+++ b/net/minecraft/world/level/chunk/ChunkAccess.java
@@ -73,10 +_,212 @@
     protected final @Nullable BlendingData blendingData;
     public final Map<Heightmap.Types, Heightmap> heightmaps = Maps.newEnumMap(Heightmap.Types.class);
     // Paper - rewrite chunk system
-    private final Map<Structure, StructureStart> structureStarts = Maps.newHashMap();
-    private final Map<Structure, LongSet> structuresRefences = Maps.newHashMap();
-    protected final Map<BlockPos, CompoundTag> pendingBlockEntities = Maps.newHashMap();
-    public final Map<BlockPos, BlockEntity> blockEntities = new Object2ObjectOpenHashMap<>();
+    private final Map<Structure, StructureStart> structureStarts = new it.unimi.dsi.fastutil.objects.Object2ObjectArrayMap<>(); // Canvas - optimized collection
+    private final Map<Structure, LongSet> structuresRefences = new it.unimi.dsi.fastutil.objects.Object2ObjectArrayMap<>(); // Canvas - optimized collection
+    protected final Map<BlockPos, CompoundTag> pendingBlockEntities = new Object2ObjectOpenHashMap<>(); // Canvas - optimized collection
+    // Canvas start - optimize block entity fetching
+    /*
+      When the chunk has a lot of block entities, this is much faster
+      to use than the previous 'Map<BlockPos, BlockEntity> blockEntities'
+     */
+    // public final Map<BlockPos, BlockEntity> blockEntities = new Object2ObjectOpenHashMap<>();
+    private static final BlockEntity[] EMPTY_ARRAY = new BlockEntity[0];
+    private static final int SECTION_SIZE = 16 * 16;
+    protected final BlockEntity[][] canvas$blockEntitySections;
+    // Rolia start - O(1) block-entity count and per-section occupancy
+    // canvas$getAllBlockEntities() used to walk every bucket twice: once through
+    // canvas$getBlockEntitiesCount() to size the result array, then a second time to fill it. And
+    // canvas$removeFromBuckets() rescanned a whole 256-slot bucket after every single removal, just
+    // to decide whether that bucket had become empty. Both are now O(1) off two incrementally
+    // maintained counters.
+    //
+    // Iteration ORDER is deliberately untouched: every loop below still walks sections from low Y
+    // to high Y and, within a section, in ((x & 15) | ((z & 15) << 4)) index order. That order is
+    // observable through CraftChunk#getTileEntities() and ClientboundLevelChunkPacketData, so this
+    // stays an array of buckets and does not become a dense list or a map.
+    //
+    // Invariants, established in the constructor and maintained by the four mutators below --
+    // canvas$setBlockEntityInBuckets, canvas$removeFromBuckets and the two canvas$removeAll
+    // overloads are the only writers of canvas$blockEntitySections anywhere in the tree:
+    //   canvas$blockEntityCount       == number of non-null slots across every bucket
+    //   canvas$sectionEntityCounts[i] == number of non-null slots in canvas$blockEntitySections[i],
+    //                                    and is 0 exactly when that bucket reference is null
+    private int canvas$blockEntityCount;
+    private final short[] canvas$sectionEntityCounts; // at most 256 per section, fits in a short
+    // Rolia end - O(1) block-entity count and per-section occupancy
+    private final int canvas$minY;
+    private final int canvas$maxY;
+    private final int canvas$height;
+
+    /**
+     * This fetches block entity buckets from the global block coordinates
+     * <br></br>
+     * If there is no bucket at the y location we quick return null,
+     * since there shouldn't be a block entity there if there is no bucket
+     * <br></br>
+     * @param globalX global x
+     * @param y global y or local y
+     * @param globalZ global z
+     * @return nullable block entity at the local position
+     */
+    @Nullable
+    public BlockEntity canvas$getFromBuckets(final int globalX, final int y, final int globalZ) {
+        if (y < this.canvas$minY || y > this.canvas$maxY) return null;
+        BlockEntity[] bucket = canvas$blockEntitySections[canvas$toSectionIndex(y)];
+        if (bucket == null) return null;
+        return bucket[canvas$sectionIndex(globalX, globalZ)];
+    }
+
+    private Map<BlockPos, BlockEntity> canvas$cachedBlockEntityMap = null;
+
+    @Nullable
+    // Note: if removing a block entity, don't use this, use remove method. The block entity is expected non-null
+    public BlockEntity canvas$setBlockEntityInBuckets(final int globalX, final int y, final int globalZ, final BlockEntity blockEntity) {
+        if (y < this.canvas$minY || y > this.canvas$maxY) return null;
+        int sectionIndex = canvas$toSectionIndex(y);
+        BlockEntity[] bucket = canvas$blockEntitySections[sectionIndex];
+        if (bucket == null) {
+            bucket = new BlockEntity[SECTION_SIZE];
+            canvas$blockEntitySections[sectionIndex] = bucket;
+        }
+        int i = canvas$sectionIndex(globalX, globalZ);
+        BlockEntity old = bucket[i];
+        bucket[i] = blockEntity;
+        // Rolia start - keep the counters in sync
+        // Mirrors what actually landed in the slot, including the contract-violating null argument
+        // the javadoc above warns against, so the counters can never disagree with the array.
+        if (old == null) {
+            if (blockEntity != null) {
+                ++this.canvas$blockEntityCount;
+                ++this.canvas$sectionEntityCounts[sectionIndex];
+            }
+        } else if (blockEntity == null) {
+            --this.canvas$blockEntityCount;
+            --this.canvas$sectionEntityCounts[sectionIndex];
+        }
+        // Rolia end - keep the counters in sync
+        this.canvas$cachedBlockEntityMap = null;
+        return old;
+    }
+
+    public void canvas$removeAll(final int y) {
+        if (y < this.canvas$minY || y > this.canvas$maxY) return;
+        // Rolia start - keep the counters in sync
+        final int sectionIndex = canvas$toSectionIndex(y);
+        this.canvas$blockEntityCount -= this.canvas$sectionEntityCounts[sectionIndex];
+        this.canvas$sectionEntityCounts[sectionIndex] = 0;
+        canvas$blockEntitySections[sectionIndex] = null;
+        // Rolia end - keep the counters in sync
+        this.canvas$cachedBlockEntityMap = null;
+    }
+
+    public void canvas$removeAll() {
+        java.util.Arrays.fill(canvas$blockEntitySections, null);
+        // Rolia start - keep the counters in sync
+        java.util.Arrays.fill(this.canvas$sectionEntityCounts, (short) 0);
+        this.canvas$blockEntityCount = 0;
+        // Rolia end - keep the counters in sync
+        this.canvas$cachedBlockEntityMap = null;
+    }
+
+    @Nullable
+    public BlockEntity canvas$removeFromBuckets(final int globalX, final int y, final int globalZ) {
+        if (y < this.canvas$minY || y > this.canvas$maxY) return null;
+        int sectionIndex = canvas$toSectionIndex(y);
+        BlockEntity[] bucket = canvas$blockEntitySections[sectionIndex];
+        if (bucket != null) {
+            int i = canvas$sectionIndex(globalX, globalZ);
+            BlockEntity removed = bucket[i];
+            bucket[i] = null;
+            this.canvas$cachedBlockEntityMap = null;
+
+            // Rolia start - O(1) emptiness test instead of rescanning the whole 256-slot bucket
+            // Exactly the old semantics: drop the bucket reference iff no non-null slot is left in
+            // it after this removal, and return the removed value either way.
+            if (removed != null) {
+                --this.canvas$blockEntityCount;
+                --this.canvas$sectionEntityCounts[sectionIndex];
+            }
+            if (this.canvas$sectionEntityCounts[sectionIndex] > 0) return removed;
+            // Rolia end - O(1) emptiness test instead of rescanning the whole 256-slot bucket
+
+            // doesn't have anymore entries, remove from buckets
+            canvas$blockEntitySections[sectionIndex] = null;
+            return removed;
+        } // else | no need to remove anything, there is no bucket
+        return null;
+    }
+
+    private int canvas$sectionIndex(final int globalX, final int globalZ) {
+        return (globalX & 15) | ((globalZ & 15) << 4);
+    }
+
+    private int canvas$toSectionIndex(final int y) {
+        int idx = y - canvas$minY;
+        if (idx < 0 || idx >= canvas$height) {
+            throw new IndexOutOfBoundsException("Y=" + y + " out of range (" + canvas$minY + " to " + canvas$maxY + ")");
+        }
+        return idx;
+    }
+
+    public int canvas$getBlockEntitiesCount() {
+        return this.canvas$blockEntityCount; // Rolia - O(1); was a full scan of all 384 buckets
+    }
+
+    public BlockEntity[] canvas$getAllBlockEntities() {
+        final int total = this.canvas$blockEntityCount; // Rolia - O(1); no separate counting pass
+
+        if (total == 0) return EMPTY_ARRAY;
+
+        BlockEntity[] result = new BlockEntity[total];
+        int idx = 0;
+
+        for (BlockEntity[] bucket : this.canvas$blockEntitySections) {
+            if (bucket == null) continue;
+
+            for (BlockEntity blockEntity : bucket) {
+                if (blockEntity != null) {
+                    result[idx++] = blockEntity;
+                    if (idx == total) return result; // Rolia - all found, skip the empty tail
+                }
+            }
+        }
+
+        // Rolia - defensive: reachable only if a counter ever drifted. Hand back exactly what was
+        // Rolia - found rather than an array padded with nulls. Dead code while the invariant holds.
+        return idx == total ? result : java.util.Arrays.copyOf(result, idx);
+    }
+
+    public boolean canvas$containsPos(final int globalX, final int y, final int globalZ) {
+        if (y < this.canvas$minY || y > this.canvas$maxY) return false;
+        BlockEntity[] bucket = canvas$blockEntitySections[canvas$toSectionIndex(y)];
+        if (bucket == null) return false;
+        return bucket[canvas$sectionIndex(globalX, globalZ)] != null;
+    }
+
+    public boolean canvas$containsPos(final BlockPos pos) {
+        return canvas$containsPos(pos.getX(), pos.getY(), pos.getZ());
+    }
+
+    public void canvas$iterateOverBlockEntities(final java.util.function.Consumer<BlockEntity> consumer) {
+        for (BlockEntity[] bucket : canvas$blockEntitySections) {
+            if (bucket == null) continue;
+            for (final BlockEntity blockEntity : bucket) {
+                if (blockEntity == null) continue;
+                consumer.accept(blockEntity);
+            }
+        }
+    }
+
+    public Map<BlockPos, BlockEntity> canvas$buildBlockEntityMap() {
+        if (this.canvas$cachedBlockEntityMap != null) return this.canvas$cachedBlockEntityMap;
+        Map<BlockPos, BlockEntity> blockEntities = new Object2ObjectOpenHashMap<>();
+        this.canvas$iterateOverBlockEntities((blockEntity) -> {
+            blockEntities.put(blockEntity.getBlockPos(), blockEntity);
+        });
+        return this.canvas$cachedBlockEntityMap = java.util.Collections.unmodifiableMap(blockEntities);
+    }
+    // Canvas end - optimize block entity fetching
     protected final LevelHeightAccessor levelHeightAccessor;
     protected final LevelChunkSection[] sections;
     // CraftBukkit start - SPIGOT-6814: move to ChunkAccess to account for 1.17 to 1.18 chunk upgrading.
@@ -172,6 +_,13 @@
         this.minSection = ca.spottedleaf.moonrise.common.util.WorldUtil.getMinSection(levelHeightAccessor);
         this.maxSection = ca.spottedleaf.moonrise.common.util.WorldUtil.getMaxSection(levelHeightAccessor);
         // Paper end - get block chunk optimisation
+        // Canvas start - optimize block entity fetching
+        this.canvas$minY = this.getMinY();
+        this.canvas$maxY = this.getMaxY();
+        this.canvas$height = canvas$maxY - canvas$minY + 1;
+        this.canvas$blockEntitySections = new BlockEntity[this.canvas$height][];
+        this.canvas$sectionEntityCounts = new short[this.canvas$height]; // Rolia - per-section occupancy counters
+        // Canvas end - optimize block entity fetching
     }
 
     private void replaceMissingSections(final PalettedContainerFactory containerFactory, final LevelChunkSection[] sections) { // Paper - Anti-Xray - make it a non-static method
@@ -219,7 +_,18 @@
 
     public Set<BlockPos> getBlockEntitiesPos() {
         Set<BlockPos> result = Sets.newHashSet(this.pendingBlockEntities.keySet());
-        result.addAll(this.blockEntities.keySet());
+        // Canvas start - optimize block entities
+        for (int y = 0; y < canvas$blockEntitySections.length; y++) {
+            BlockEntity[] bucket = canvas$blockEntitySections[y];
+            if (bucket == null) continue;
+
+            for (int i = 0; i < bucket.length; i++) {
+                BlockEntity be = bucket[i];
+                if (be == null) continue;
+                result.add(be.getBlockPos());
+            }
+        }
+        // Canvas end - optimize block entities
         return result;
     }
 
@@ -277,7 +_,7 @@
     }
 
     public Map<Structure, StructureStart> getAllStarts() {
-        return Collections.unmodifiableMap(this.structureStarts);
+        return this.structureStarts; // Canvas - optimized collection
     }
 
     public void setAllStarts(final Map<Structure, StructureStart> starts) {
@@ -299,7 +_,7 @@
 
     @Override
     public Map<Structure, LongSet> getAllReferences() {
-        return Collections.unmodifiableMap(this.structuresRefences);
+        return this.structuresRefences; // Canvas - optimized collection
     }
 
     @Override
@@ -374,7 +_,7 @@
 
     public void setBlockEntityNbt(final CompoundTag entityTag) {
         BlockPos posFromTag = BlockEntity.getPosFromTag(this.chunkPos, entityTag);
-        if (!this.blockEntities.containsKey(posFromTag)) {
+        if (!this.canvas$containsPos(posFromTag)) { // Canvas - optimize block entities
             this.pendingBlockEntities.put(posFromTag, entityTag);
         }
     }
